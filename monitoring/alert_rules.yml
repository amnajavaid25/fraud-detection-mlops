groups:
- name: fraud-alerts

  rules:

  - alert: LowRecall
    expr: fraud_recall < 0.85
    for: 1m

    annotations:
      summary: "Fraud recall dropped"

  - alert: HighDrift
    expr: data_drift > 0.7
    for: 1m

    annotations:
      summary: "Data drift detected"